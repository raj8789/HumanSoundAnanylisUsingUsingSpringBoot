package com.project.sound.HumanSoundDetection.HumanSoundDetection.service;


import org.tensorflow.Tensor;
import org.tensorflow.ndarray.Shape;
import org.tensorflow.types.TFloat32;

public class WebRTCVoiceActivityDetector {
//    private static final WebRtcVad vad = new WebRtcVad();
//
//    public static boolean isLiveSpeech(byte bytesRead[]) {
//        vad.setMode(3);  // Most aggressive filtering
//        return vad.process(16000, buffer, 0, bytesRead.length);
//    }
//    private float[] extractMFCC(byte[] buffer) {
//        float[] audioFeatures = new float[buffer.length];
//        for (int i = 0; i < buffer.length; i++) {
//            audioFeatures[i] = buffer[i] / 32768.0f; // Normalize to range [-1, 1]
//        }
//        return audioFeatures;
//    }
}
